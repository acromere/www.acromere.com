export default class Config {

    static ROOT_URL = 'https://www.acromere.com';
    static DOWNLOAD_URL = this.ROOT_URL + '/download';
    static ICON_ROOT = this.DOWNLOAD_URL;
    static IMAGE_ROOT = this.ROOT_URL;
    static SCREENSHOT_ROOT = this.IMAGE_ROOT + '/screenshots';

}
